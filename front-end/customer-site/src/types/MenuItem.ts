type MenuItem = {
  productId?: string;
  productName: string;
  imageUrl: string;
  description: string;
  categoryId: number;
  price: string | number;
  quantity: string | number;
}

export default MenuItem;